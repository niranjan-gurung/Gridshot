#include "Game.h"

//#include <iostream>
//#include <random>

int main() 
{
	/*std::random_device dev;
	std::mt19937 rng(dev());

	for (int i = 0; i < 10; i++)
	{
		int num = std::uniform_int_distribution<uint32_t>{1, 20}(rng);
		std::cout << num << std::endl;
	}*/


    Game game;
    game.Start();
}
